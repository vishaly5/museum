# Toy and Doll Gallery audio

One subfolder per language, e.g. `Hindi/`, `Telgu/`, `Urdu/`.
Reference the files from `js/artworksData.js` as `audio/Hindi/item1.wav`.

Leave `audioSrc` empty for a language until its real narration exists -
never point it at another language's file.
0